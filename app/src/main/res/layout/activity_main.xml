<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="gj.meteoras.ui.places.PlacesViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="@style/Places.Container">

        <EditText
            android:id="@+id/filter"
            android:singleLine="true"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:onTextChanged="@{(text) -> viewModel.filter(text)}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/progress" />

        <ProgressBar
            android:id="@+id/progress"
            style="@style/Places.Progress"
            app:visible="@{viewModel.state.busy}"
            app:layout_constraintTop_toTopOf="@id/filter"
            app:layout_constraintBottom_toBottomOf="@id/filter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/filter" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/spacing_vertical"
            app:layout_constraintTop_toBottomOf="@id/filter"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:listitem="@layout/item_place"
            tools:itemCount="20"
            app:listItems="@{viewModel.state.places}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
